# Docker Starter Kit for Laravel

A Docker configuration package for Laravel applications with development and production environments.

## Installation

```bash
composer require parsa-mirzaie/docker-starter-kit-laravel